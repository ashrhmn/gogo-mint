version: "3"

services:
  client:
    build:
      context: .
      args:
        - PORT=${PORT:-3001}
    image: gogo-mint-dftsw-2-img
    container_name: gogo-mint-dftsw-2
    restart: always
networks:
  default:
    external: true
    name: nginx
